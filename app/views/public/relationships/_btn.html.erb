<% if current_user %>
  <% unless user.guest_user? || current_user.guest_user? %>
    <% if current_user.following?(user) %>
      <%= link_to "フォロー解除", user_relationships_path(user.id), method: :delete, class: "btn btn-info btn-block" %>
    <% else %>
      <%= link_to "フォローする", user_relationships_path(user.id), method: :post, class: "btn btn-secondary btn-block" %>
    <% end %>
  <% end %>
<% end %>