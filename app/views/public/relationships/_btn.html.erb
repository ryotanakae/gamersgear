<% if current_user %>
<!--現在のユーザーが存在する場合-->
  <% unless user.guest_user? || current_user.guest_user? %>
  <!-- 相手または自分がゲストユーザーでない場合（相手または自分がゲストの場合は空白） -->
    <% if current_user.following?(user) %>
    <!--現在のユーザーが対象ユーザーをフォローしている場合-->
      <%= link_to "フォロー解除", user_relationships_path(user.id), method: :delete, remote: true, class: "btn btn-info btn-block" %>
    <% else %>
    <!--フォローしていない場合-->
      <%= link_to "フォローする", user_relationships_path(user.id), method: :post, remote: true,  class: "btn btn-secondary btn-block" %>
    <% end %>
  <% end %>
<% end %>